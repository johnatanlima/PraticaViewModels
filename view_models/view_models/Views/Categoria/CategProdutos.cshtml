@model IEnumerable<view_models.Models.Produto>

@{
    ViewData["Title"] = "Index2";
    var total = ViewBag.listapreco;
    var total222 = ViewBag.soma22;
    decimal total3=0;
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New Produto view model</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Preco)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categoria)
            </th>
            <th>
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        total3 = total3 + item.Preco;
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Preco)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categoria.Nome)
            </td>
            <td>
                <a asp-controller="Produto" asp-action="Edit" asp-route-id="@item.ProdutoId">Edit</a> |
                <a asp-controller="Produto" asp-action="Details" asp-route-id="@item.ProdutoId">Details</a> |
                <a asp-controller="Produto" asp-action="Delete" asp-route-id="@item.ProdutoId">Delete</a>
            </td>
        </tr>
}
        <tr> 
            <td>
                <label> Total </label>
            </td>
            <td>  @total     </td>
            <td>  @total222  </td> 
            <td>  @total3    </td>      
        </tr>
    </tbody>
</table>